services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: matarplan
      POSTGRES_PASSWORD: matarplan
      POSTGRES_DB: matarplan
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
